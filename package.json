{
  "name": "betterclaw-project",
  "version": "0.3.10",
  "private": true,
  "description": "Workflow-enforced AI agents for OpenClaw (and soon Cowork). Paragraph-in, graph-out, workflow-enforced.",
  "scripts": {
    "setup": "pnpm install",
    "install-plugin": "openclaw plugins install $PWD/packages/plugin-openclaw --link",
    "doctor": "./packages/cli/bin/betterclaw doctor",
    "test": "node --test packages/plugin-openclaw/test packages/cli/test"
  },
  "engines": {
    "node": ">=22",
    "pnpm": ">=9"
  },
  "packageManager": "pnpm@9.0.0"
}
